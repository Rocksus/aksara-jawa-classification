syntax = "proto3";

package classifier;

message Prediction {
    int32 classID = 1;
    string name = 2;
    float confidence = 3;
}

message Parameters {
    bytes data = 1;
}

service AksaraJawaClassifier {
    rpc Classify(Parameters) returns (Prediction) {}
}